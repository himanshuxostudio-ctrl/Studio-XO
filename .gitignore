.next/
node_modules/
.env
.env.local
.env*.local
npm-debug.log*
.DS_Store
*.tsbuildinfo
next-env.d.ts

# All runtime data now lives in Supabase (see supabase/schema.sql). The
# data/*.json files that remain are the original migration source only —
# see scripts/migrate-to-supabase.mjs and the README's "Data migration".
